# 🚀 Быстрый старт ProfiTech

## Шаг 1: Запуск проекта

```bash
cd profitech-catalog
npm run dev
```

Откройте браузер: http://localhost:3000

## Шаг 2: Загрузка контента

### 1. Логотип
Поместите ваш логотип в:
```
profitech-catalog/public/uploads/logo/logo.png
```

### 2. Изображения для карусели
Добавьте изображения (1920x1080px) в:
```
profitech-catalog/public/uploads/carousel/
- slide1.jpg
- slide2.jpg
- slide3.jpg
```

### 3. Каталог товаров

#### Вариант A: Загрузка через админ-панель
1. Откройте http://localhost:3000/admin
2. Перейдите на вкладку "Каталог"
3. Загрузите ваш CSV файл

#### Вариант B: Пример CSV для тестирования
Уже создан пример: `public/uploads/csv/example-catalog.csv`

### Формат CSV файла:
```csv
id,name,description,categoryId,subcategoryId,manufacturer,images,char_мощность,char_объем
1,"Товар 1","Описание","2","2-2","Brand","img.jpg","1200W","2L"
```

**Важно:**
- `categoryId` - от 1 до 6
- `subcategoryId` - формат: "categoryId-number" (например: "1-1", "2-5")
- `images` - пути к изображениям через запятую
- `char_*` - любые характеристики с префиксом "char_"

## Шаг 3: Настройка контактов

Отредактируйте файл `constants/categories.ts`:
```typescript
export const COMPANY_INFO = {
  name: 'ProfiTech',
  slogan: 'От идеи до воплощения',
  defaultWhatsApp: '+79999999999', // ВАШ НОМЕР
};
```

## Шаг 4: Проверка функционала

✅ Главная страница - карусель, категории
✅ Каталог - фильтры, поиск
✅ Корзина - добавление товаров
✅ WhatsApp - оформление заказа
✅ AI Помощник - чат-бот (правый нижний угол)
✅ Админ-панель - управление каталогом

## 📱 Тестирование мобильной версии

1. Откройте DevTools (F12)
2. Переключите на мобильный вид
3. Проверьте все страницы

## 🤖 Настройка AI (опционально)

Для подключения OpenAI:

1. Получите API ключ: https://platform.openai.com/api-keys
2. Создайте `.env.local`:
```env
OPENAI_API_KEY=sk-...
```
3. Установите: `npm install openai`
4. Раскомментируйте код в `app/api/chat/route.ts`

## 🎨 Структура проекта

```
profitech-catalog/
├── app/                    # Страницы Next.js
│   ├── page.tsx           # Главная
│   ├── catalog/           # Каталог
│   ├── cart/              # Корзина
│   ├── about/             # О компании
│   ├── contacts/          # Контакты
│   ├── admin/             # Админ-панель
│   └── api/chat/          # API для AI
├── components/            # React компоненты
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── ProductCard.tsx
│   ├── Carousel.tsx
│   └── AIAssistant.tsx
├── store/                 # Zustand stores
├── types/                 # TypeScript типы
├── constants/             # Константы
└── public/uploads/        # Загружаемые файлы
    ├── csv/
    ├── logo/
    └── carousel/
```

## 🔧 Полезные команды

```bash
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для продакшена
npm start        # Запуск продакшен-сервера
npm run lint     # Проверка кода
```

## ❗ Частые вопросы

### Как изменить цветовую схему?
Отредактируйте `app/globals.css` - секция `:root`

### Как добавить новую категорию?
Добавьте в `constants/categories.ts` в массив `CATEGORIES`

### Куда загружать изображения товаров?
В папку `public/products/` или любую в `public/`

### Как работает WhatsApp интеграция?
При оформлении заказа формируется сообщение с товарами и открывается WhatsApp

## 📞 Поддержка

Все работает? Отлично! 🎉

Возникли вопросы? Проверьте README.md

---

**ProfiTech** - Готов к запуску! 🚀

