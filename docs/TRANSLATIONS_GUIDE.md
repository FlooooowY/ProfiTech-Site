# Быстрое добавление переводов

## Как это работает

1. **Переводы хранятся в** `messages/ru.json` и `messages/en.json`
2. **Используйте хук** `useTranslations()` в компонентах
3. **Переключатель языка** уже добавлен в Header

## Быстрое добавление переводов в компонент

### Шаг 1: Добавьте переводы в JSON файлы

В `messages/ru.json` и `messages/en.json` добавьте нужные ключи:

```json
{
  "catalog": {
    "title": "Каталог товаров",
    "filters": "Фильтры",
    "addToCart": "В корзину"
  }
}
```

### Шаг 2: Используйте в компоненте

```tsx
'use client';

import { useTranslations } from '@/lib/i18n';

export default function MyComponent() {
  const t = useTranslations();
  
  return (
    <div>
      <h1>{t('catalog.title')}</h1>
      <button>{t('catalog.addToCart')}</button>
    </div>
  );
}
```

## Примеры для основных компонентов

### Каталог (app/catalog/page.tsx)

```tsx
const t = useTranslations();

// Замените:
"Фильтры" → t('catalog.filters')
"Производители" → t('catalog.manufacturers')
"В корзину" → t('catalog.addToCart')
```

### Корзина (app/cart/page.tsx)

```tsx
const t = useTranslations();

// Замените:
"Корзина" → t('cart.title')
"Итого" → t('cart.total')
"Оформить заказ" → t('cart.order')
```

### Контакты (app/contacts/page.tsx)

```tsx
const t = useTranslations();

// Замените:
"Контакты" → t('contacts.title')
"Адрес" → t('contacts.address')
"Телефон" → t('contacts.phone')
```

## Структура переводов

Используйте вложенную структуру для организации:

```json
{
  "namespace": {
    "key": "Значение"
  }
}
```

Например:
- `home.title` - заголовок главной страницы
- `catalog.filters` - фильтры в каталоге
- `cart.total` - итого в корзине

## Переключатель языка

Переключатель языка уже добавлен в Header и работает автоматически. Язык сохраняется в `localStorage`.

## Добавление новых языков

1. Создайте файл `messages/[код].json` (например, `messages/de.json`)
2. Добавьте код языка в `components/LanguageSwitcher.tsx`:

```tsx
const languages = [
  { code: 'ru', name: 'Русский', flag: '🇷🇺' },
  { code: 'en', name: 'English', flag: '🇬🇧' },
  { code: 'de', name: 'Deutsch', flag: '🇩🇪' }, // Новый язык
];
```

3. Обновите `lib/i18n.ts`:

```tsx
import de from '@/messages/de.json';

const translations = {
  ru,
  en,
  de, // Новый язык
};
```

## Мобильная адаптация

Все компоненты уже адаптированы для мобильных устройств с помощью Tailwind CSS классов:
- `md:` - для планшетов и больше
- `lg:` - для десктопов
- `sm:` - для больших мобильных

Переключатель языка автоматически адаптируется на мобильных устройствах.

