# Быстрый перевод товаров через локальный сервер

Для перевода 126k товаров за 10-15 минут используйте локальный сервер перевода LibreTranslate.

## Вариант 1: Docker (рекомендуется)

### Установка LibreTranslate через Docker:

```bash
# Запустите LibreTranslate в Docker
docker run -ti --rm -p 5000:5000 libretranslate/libretranslate

# Или в фоновом режиме
docker run -d -p 5000:5000 --name libretranslate libretranslate/libretranslate
```

### Использование:

```bash
cd ~/ProfiTech-Site
npm run db:translate-products-fast
```

## Вариант 2: Публичный API (с лимитами)

Если не можете установить Docker, используйте публичный API:

```bash
# Установите переменную окружения
export LIBRETRANSLATE_URL=https://libretranslate.com

# Запустите скрипт
npm run db:translate-products-fast
```

**Внимание:** Публичный API имеет лимиты, поэтому скорость будет ниже.

## Вариант 3: Установка LibreTranslate на сервере

### Установка через pip:

```bash
# Установите Python 3.8+
sudo apt update
sudo apt install python3-pip

# Установите LibreTranslate
pip3 install libretranslate

# Запустите сервер
libretranslate --host 0.0.0.0 --port 5000
```

## Производительность

- **Локальный сервер (Docker):** ~100-200 товаров/секунду
- **Публичный API:** ~10-20 товаров/секунду (из-за лимитов)

Для 126k товаров:
- Локальный: ~10-20 минут
- Публичный API: ~2-3 часа

## Особенности скрипта

1. **Кэширование:** Одинаковые тексты переводятся один раз
2. **Параллельная обработка:** 50 товаров одновременно
3. **Batch обновления:** Обновления в MongoDB батчами
4. **Автоматическое восстановление:** При ошибках продолжает работу

## Мониторинг

Скрипт показывает прогресс каждые 100 товаров:
```
📊 Прогресс: 45.2% (45200/100000) | Обновлено: 45000 | Кэш: 12000 | Ошибок: 5
```

## Остановка и возобновление

Скрипт можно остановить (Ctrl+C) и запустить снова - он продолжит с непереведенных товаров.

